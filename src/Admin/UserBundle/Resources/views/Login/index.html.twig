{% extends ':admin.html.twig' %}

{% block pageTitle %}
    Login
{% endblock %}

{% block body %}
    <h1>Login</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

{% endblock %}
